services:
  srt-backend:
    build: .
    ports:
      - "3000:3000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
      - PORT=3000
    volumes:
      # Persist job data and logs across restarts
      - ./data:/app/src/data
    restart: unless-stopped
