version: "3.9"

services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      HOST: 0.0.0.0
      NODE_ENV: production
      LOG_LEVEL: info
    volumes:
      - srt_data:/app/data
    restart: unless-stopped

volumes:
  srt_data: